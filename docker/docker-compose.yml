services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: imw-app
    ports:
      - "8001:80"
    volumes:
      - ..:/var/www/html
    working_dir: /var/www/html